var models;
(function (models) {
    var score;
    (function (score) {
        /** 游戏得分数据模型 */
        var ScoreInfoVO = /** @class */ (function () {
            function ScoreInfoVO() {
            }
            return ScoreInfoVO;
        }());
        score.ScoreInfoVO = ScoreInfoVO;
    })(score = models.score || (models.score = {}));
})(models || (models = {}));
